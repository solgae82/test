substring(startIndex, endIndex) : 
	음수 인덱스 사용 불가, 끝 인덱스를 포함하지 않음. 두 인자의 순서가 바뀌어도 동일한 결과 반환.

substr(start, length) : 
	뒤에서부터 음수 인덱스 사용 가능, 시작 인덱스부터 지정된 length 길이만큼 추출. 
	현재는 폐기된 함수이므로 사용 지양.(x)

slice(start, end) : 
	뒤에서부터 음수 인덱스 사용 가능. substring()과 유사한 동작을 하지만 음수 인덱스 지원이 다름.