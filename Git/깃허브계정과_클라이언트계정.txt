항상 이게 궁금했다

로컬(클라이언트) pc의 깃 기본 정보를 셋팅하면, 그 계정 정보가 push한 계정 정보가 되는걸까?
아니면, 원격 깃허브 연결할때 (다른) 계정정보로 올린 것으로 되는 걸까?

실험결과 
깃허브 로그인 정보는 말 그대로 로그일 할때 필요한 정보이고, 커밋 정보와는 무관하다.

깃 허브 push 할때, 연결할 정보를 요즘에 시크릿 key로 하는데, 그것만 넣어주면 그만이다.
push 한 계정 정보는 로컬pc에서 config로 등록한 'name' 정보이다.
