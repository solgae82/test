[그림으과 실습으로 배우는 도커 & 쿠버네티스] 위키북스 책 중 쿠버네티스 요약

쿠버네티스는 컨테이너 오케스트레이션 도구의 일종이다.

쿠버네티스는 여러 대의 컨테이너가 여러 대의 물리 서버에 걸쳐 실행되는 것을 전제로 한다.

쿠버네티스구조

	클러스터 
		마스터 노드
			워커 노드에서 실행되는 컨테이너를 관리하는 역할을 한다
			컨트롤 플레인 : 워커 노드 관리
				- API 서버: 
					kube-apiserver:
						외부와 통신하는 프로세스,kubectl로 명령를 전달받아 실행
				- 컨트롤 매니저: 
					kube-controller-manager:
						컨트롤러를 통합 관리, 실행
					cloud-controller-manager: 
						클라우드 서비스와 연동해 서비스 생성
				- 스케쥴러: 
					kube-scheduler: 파드를 워커 노드에 할당한다
				- etcd: 클러스터 관련 정보 전반을 관리하는 데이터베이스
		워커 노드 (물리 서버)
			컨테이너가 동작해야하는 곳
			컨테이너 엔진이 설치돼야 한다
			
			kube-let: 
				- 마스터 노드에 있는 kube-scheduler와 연동하면 워커 노드에 파드를 배치하고 실행.
				- 실행 중인 파드의 상태를 정기적으로 모니터링하며 kube-scheduler에 통지.
			kube-proxy : 네트워크 통신의 라우팅 메커니즘
			
			
쿠버네티스 철학 : 
	- 정의된 바람직한 상태(.yml)를 자동으로 생성, 모니터링, 유지
	- 유지되는 상태를 변경하고 싶다면 정의된 바람직한 상태(.yml)를 변경한다.
		(.yml을 통하지 않고 중간에 사람이 개입해서 컨테이너를 추가/삭제/변경 하면 안된다)
		
		
쿠버네티스 구성관련 중요 리소스

	- 파드 (pod)
		파드는 컨테이너+volume  묶은 것으로 이해하면 편하다
		기본적으로 파드1개 = 컨테이너 1개 이지만, 컨테이너가 여러개인 파드가 있을 수도 있다
		
	- 서비스 (service)
		파드의 모임을 서비스라 한다. 파드를 이끄는 반장이라고 생각하자
		서비스 요청 로드밸런서 역할을 한다
		서비스 아래 파드는 모두 같은 구성을 갖는다
		다른 구성을 갖는 파드는 다른 서비스로 관리한다
		파드가 여러개의 워커 노드에(물리적 서버)에 걸쳐 동작하더라도 모두 서비스가 관리한다.
		
	- 레플리카세트(replicaset)
		파드 수를 관리하는 반장
		레플리카세트가 관리하는 동일한 파드를 'replica' 라고 한다
		단독으로 쓰이기 보다는, 디플로이먼트와 함께 쓰인다.
		
	- 디플로이먼트 (Deployment)
		파드의 디플로이(배포)를 관리한다
		레플리카세트 위에 있는 상사

위의 4개 중요 리소스 말고도 모두 합해 50여종이 넘늗다고 한다.
일단 위 중요 4개 리소스만 기억하자



