DDL (Data Definition Language) : 데이터 정의어
	
	CREATE
	ALTER
	DROP
	RENAME
	TRUNCATE
	
DML (Data Manipaulation Language) : 데이터 조작어
	
	SELECT
	INSERT
	UPDATE
	DELETE
	
DCL (Data Control Language) : 데이터 제어어
	
	GRANT
	REVOKE
	
TCL (Transaction Control Language) : 트랜잭션 제어어
	
	COMMIT
	ROLLBACK
	SAVEPOINT